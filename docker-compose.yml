version: '3.1'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - /var/lib/mysql:/var/lib/mysql
    restart: always
    ports:
      - 3306:3306
    environment:
      - MYSQL_DATABASE=amire
      - MYSQL_USER=amire
      - MYSQL_PASSWORD=h1b3rn4t3
      - MYSQL_ROOT_PASSWORD=h1b3rn4t3

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080
    environment:
      - ADMINER_DESIGN=pepa-linha